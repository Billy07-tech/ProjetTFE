<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Checkers Elite{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <style>
        body {
            font-family: 'Roboto';
            margin: 0; 
            padding: 0; 
            background-color: #E6DfC6; 
            position: relative; 
            min-height: 100vh; 
        }
        menu {
            position: absolute; 
            top: 10px; 
            width: 100%; 
            padding: 10px 0; 
            text-align: right; 
        }
        menu ul {
            list-style: none;
            margin: 0;
            padding: 0;
            text-align: right; 
        }
        menu ul li {
            display: inline; 
            margin-right: 110px; 
        }
        menu ul li a {
            text-decoration: none;
            color: inherit; 
        }
        menu ul li a:hover {
            text-decoration: none; 
            font-weight: bold;
        }
        .assistance {
            text-decoration: none;
            color: inherit;
            position: absolute; 
            bottom: 10px; 
            left: 20px;
        }
        .assistance:hover {
            text-decoration: none; 
            font-weight: bold;
        }
    </style>
    {% block stylesheets %}
    {% endblock %}
    
    {% block head %}
</head>
<body>

    <a href="{{ path('app_accueil') }}"><img src="{{ asset('logo.png') }}" alt="Le logo du site"/></a>
    <menu>
        <ul>
            <li><a href="{{ path('app_accueil') }}">ACCUEIL</a></li>
            <li>ACTUALITES</li>
            <li><a href="{{ path('app_regle') }}">REGLES</a></li>
            <li><a href="{{ path('app_produit_index') }}">BOUTIQUE</a></li>
            
            {# Vérifier si l'utilisateur est connecté #}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('app_administrateur') }}">ADMIN</a></li>
                {% endif %}
                <li><a href="/logout">LOGOUT</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}">LOGIN</a></li>
            {% endif %}
            
        </ul>
    </menu>
    {% endblock %}
    {% block body %}
        {% if is_granted('ROLE_ADMIN') %}
            {# Contenu normal de la page #}
            {% block content %}{% endblock %}
        {% else %}
            <p>Vous n'avez pas la permission d'accéder à cette page.</p>
        {% endif %}
    {% endblock %}

    {% block foot %}
    <div>
        <a class="assistance" href="{{ path('app_assistance') }}">ASSISTANCE</a>
    </div>
    
    {% endblock %}
</body>
</html>
