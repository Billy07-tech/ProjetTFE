{% extends 'base.html.twig' %}

{% block title %}Accueil — Checkers Elite
{% endblock %}

{% block body %}
	<section class="hero">
		<div>
			<h1 class="h-title">Jouez aux dames en ligne<br>avec style et fluidité.</h1>
			<p class="h-sub">Matchs rapides, classement, parties privées entre amis. Un design propre pour un TFE solide.</p>
			<div style="display:flex; gap:12px; flex-wrap:wrap;">
				<a class="btn" href="#">Lancer une partie</a>

				<a class="btn" href="#">Voir le classement</a>
			</div>
		</div>
		<div class="hero-card">
			<div class="board-preview">
				{% for r in 0..7 %}
					{% for c in 0..7 %}
						{% set dark = (r is odd and c is even) or (r is even and c is odd) %}
						<div class="square {{ dark ? 'dark' : 'light' }}"></div>
					{% endfor %}
				{% endfor %}
			</div>
		</div>
	</section>

	<section class="grid">
		<div class="card">
			<h3>Partie rapide</h3>
			<p class="h-sub">Entrez et jouez contre un adversaire aléatoire.</p>
			<a class="btn" href="#">Trouver un adversaire</a>
		</div>
		<div class="card">
			<h3>Jouer avec un ami</h3>
			<p class="h-sub">Créez une salle privée et partagez le lien.</p>
			<a class="btn" href="#">Créer une salle</a>
		</div>
		<div class="card">
			<h3>Règles & tutoriel</h3>
			<p class="h-sub">Tout ce qu’il faut pour bien démarrer.</p>
			<a class="btn" href="{{ path('app_regle') }}">Lire les règles</a>
		</div>
	</section>
{% endblock %}
