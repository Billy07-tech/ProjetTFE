{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block stylesheets %}
<style>
    body { font-family: 'Roboto', sans-serif; }
    .profil-container {
        max-width: 600px;
        margin: 50px auto;
        padding: 30px;
        background: #fff;
        border-radius: 15px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        text-align: center;
    }
    .profil-avatar {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid #007bff;
        margin-bottom: 15px;
    }
    .profil-info { text-align: left; margin: 20px 0; }
    .profil-info p { font-size: 1.1rem; padding: 10px; border-bottom: 1px solid #eee; }
    .profil-info strong { color: #444; }
    .btn-edit {
        display: inline-block;
        padding: 10px 20px;
        background: #007bff;
        color: #fff;
        border-radius: 8px;
        text-decoration: none;
        transition: background 0.3s ease;
    }
    .btn-edit:hover { background: #0056b3; }
</style>
{% endblock %}

{% block body %}
<div class="profil-container">
    <h1>Mon Profil</h1>

    <img src="{{ asset(user.avatar ? 'uploads/avatars/' ~ user.avatar : 'images/default-avatar.png') }}" alt="Avatar" class="profil-avatar">

    <div class="profil-info">
        <p><strong>Pseudo :</strong> {{ user.pseudo }}</p>
        <p><strong>Prénom :</strong> {{ user.prenom }}</p>
        <p><strong>Nom :</strong> {{ user.nom }}</p>
        <p><strong>Email :</strong> {{ user.email }}</p>
    </div>

    <a href="{{ path('app_profil_edit') }}" class="btn-edit">Modifier mon profil</a>
</div>
{% endblock %}
