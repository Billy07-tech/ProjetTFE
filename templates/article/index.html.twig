{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
<style>
h1 {
    text-align: center;
    color: #222;
    margin-bottom: 20px;
}

.table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

.table thead {
    background-color: #343a40;
    color: #fff;
}

.table th, .table td {
    padding: 12px 15px;
    text-align: left;
    border-bottom: 1px solid #ddd;
}

.table tr:hover {
    background-color: #f1f1f1;
}

a {
    text-decoration: none;
    color: #0d6efd;
    font-weight: bold;
}

a:hover {
    text-decoration: underline;
}

.create-btn {
    display: inline-block;
    padding: 10px 15px;
    background-color: #198754;
    color: #fff !important;
    border-radius: 6px;
    transition: background 0.2s ease;
}
.create-btn:hover {
    background-color: #146c43;
}
</style>

<h1>Liste des Articles</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Titre</th>
            <th>Contenu</th>
            <th>Auteur</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for article in articles %}
        <tr>
            <td>{{ article.id }}</td>
            <td>{{ article.titre }}</td>
            <td>{{ article.contenu|slice(0, 80) }}{% if article.contenu|length > 80 %}...{% endif %}</td>
            <td>{{ article.auteur ? article.auteur.pseudo : 'Inconnu' }}</td>
            <td>{{ article.date ? article.date|date('d/m/Y H:i') : '' }}</td>
            <td>
                <a href="{{ path('app_article_show', {'id': article.id}) }}">👁️ Voir</a>
                {% if is_granted('ROLE_ADMIN') %}
                    | <a href="{{ path('app_article_edit', {'id': article.id}) }}">✏️ Modifier</a>
                    <span>
                        {{ include('article/_delete_form.html.twig') }}
                    </span>
                {% endif %}
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="6">Aucun article trouvé</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_article_new') }}" class="create-btn">Créer un article</a>
{% endif %}
{% endblock %}
